<h1>New Boissons</h1>
<%# <%= link_to 'New Boisson', new_boissons_path %>
<% @boissonall.each do |a| %>
	<%= a.name %>
<% end %>

<%= form_for @boisson do |f| %>
  <% if @boisson.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@boisson.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @boisson.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', boissons_path %>